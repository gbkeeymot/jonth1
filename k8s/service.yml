apiVersion: v1
kind: Service
metadata:
  name: womskeir
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "womskeir"
    port: 8080
  selector:
    app: womskeir
