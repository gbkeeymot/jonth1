apiVersion: v1
kind: Service
metadata:
  name: emor
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "emor"
    port: 8080
  selector:
    app: emor
