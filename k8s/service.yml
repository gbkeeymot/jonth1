apiVersion: v1
kind: Service
metadata:
  name: fshpt
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "fshpt"
    port: 8080
  selector:
    app: fshpt
