apiVersion: v1
kind: Service
metadata:
  name: choker
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "choker"
    port: 8080
  selector:
    app: choker
